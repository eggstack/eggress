[pytest]
# Importlib mode keeps pytest from prepending source-tree directories to
# sys.path, which otherwise lets python/eggress shadow the installed wheel
# (whose compiled _eggress extension the source tree lacks).
addopts = --import-mode=importlib
