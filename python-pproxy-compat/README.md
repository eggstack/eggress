# eggress-pproxy-compat

Opt-in distribution providing the bounded top-level `pproxy` compatibility
namespace for Eggress. Install it separately from the canonical `eggress`
distribution when that namespace is required.
