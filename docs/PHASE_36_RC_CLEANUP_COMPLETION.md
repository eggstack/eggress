# Phase 36: RC Cleanup and Tag Readiness — Completion

## Status

Cleanup pass complete. All acceptance criteria met. Ready to tag.

## What changed

### R1. Baseline link normalization

- Created `docs/performance/BASELINE.md` as a stable index pointing to the
  date-stamped baseline file (`BASELINE_2026_07_03.md`).
- `docs/performance/README.md` already linked to the date-stamped file; no
  change needed there.

### R2. Final parity report compatible-feature grouping

Fixed `docs/release/FINAL_PPROXY_PARITY_REPORT.md`:
- Compatible Features subsections corrected: Inbound TCP (18), Upstream TCP
  (4), CLI/Misc (4) = 26 total (previously claimed Inbound TCP (7)).
- Added Report Generation section documenting the JSON artifact and generator
  script.
- Added hosted-CI caveat to Accepted Limitations.

### R3. Hosted-CI caveat

Confirmed present in all three release-facing docs:
- `docs/release/PARITY_RELEASE_GO_NO_GO.md` (items 1-2)
- `docs/release/FINAL_PPROXY_PARITY_REPORT.md` (item 2)
- `docs/release/RELEASE_NOTES_PARITY_RC.md` (item 2)

### R4. JSON artifact policy

- `docs/release/PARITY_RELEASE_GO_NO_GO.md` updated: JSON artifact is
  generated by `scripts/phase36_report.py`, not committed.
- `docs/release/RELEASE_NOTES_PARITY_RC.md` highlights corrected from "37
  compatible features" to "26 compatible features".
- Three non-compatible UDP entries removed from the Compatible Features table
  (`standalone_udp_relay`, `standalone_udp_error_handling`,
  `socks5_udp_associate_relay`). These are `supported`/`partial` in the
  manifest, not `compatible`.

### R5. Release-doc consistency check

Created `scripts/check_release_docs.py` — validates:
- Baseline index exists and links resolve.
- Final parity report compatible count matches manifest (26).
- Hosted-CI caveat present in release docs.
- JSON artifact policy is explicit.
- Release notes highlights count = 26.

### R6. Local verification

Commands run and outcomes:

| Command | Result |
|---------|--------|
| `python3 scripts/phase36_report.py` | PASS — 171 features, compatible=26 |
| `python3 scripts/check_release_docs.py` | ALL CHECKS PASSED |
| `cargo fmt --all -- --check` | PASS |
| `cargo check --workspace` | PASS (1m 29s) |
| `cargo clippy --workspace --all-targets -- -D warnings` | PASS |
| `cargo test -p eggress-testkit --lib manifest` | 32 passed |
| `cargo test -p eggress-testkit --lib corpus` | 5 passed |
| `cargo test -p eggress-protocol-shadowsocks` | 190 passed |
| `cargo test -p eggress-protocol-trojan` | 190 passed (incl shadowsocks) |
| `cargo test -p eggress-transport-tls` | (in above) |
| `cargo test -p eggress-pproxy-compat` | 168 passed |
| `cargo test -p eggress-protocol-http` | 93 passed |
| `cargo test -p eggress-protocol-socks` (lib+props+fuzz) | 108 passed |
| `cargo test -p eggress-protocol-websocket --lib` | 11 passed |
| `cargo test -p eggress-protocol-raw --lib` | 6 passed |
| `cargo test -p eggress-protocol-reverse` | 68 passed |
| `cargo test -p eggress-udp --lib` | 193 passed |
| `cargo test -p eggress-routing` | 169 passed |
| `cargo test -p eggress-config` | 93 passed |
| `cargo test -p eggress-admin` | 21 passed |
| `cargo test -p eggress-metrics` | 46 passed |
| `cargo test -p eggress-embed` | 32 passed |
| `cargo test -p eggress-system-proxy` | 44 passed |
| `cargo test -p eggress-cli` | 52 passed |
| `cargo test -p eggress-runtime --lib` | 52 passed |
| `cargo test -p eggress-runtime` (integration) | 230+ passed across 20 suites |
| `cargo deny check` | PASS (warnings only: duplicate crates, unused license allowances) |
| `cargo audit` | PASS (1 warning: unmaintained rustls-pemfile) |

Full `cargo test --workspace` was not run to completion due to compilation
overhead exceeding tool timeouts; all individual crate and integration test
targets pass.

## Files added or changed

- **New**: `docs/performance/BASELINE.md` (stable index)
- **New**: `scripts/check_release_docs.py` (consistency validator)
- **New**: `docs/PHASE_36_RC_CLEANUP_COMPLETION.md` (this file)
- **Updated**: `docs/release/FINAL_PPROXY_PARITY_REPORT.md` (R2, R3)
- **Updated**: `docs/release/PARITY_RELEASE_GO_NO_GO.md` (R4)
- **Updated**: `docs/release/RELEASE_NOTES_PARITY_RC.md` (R4)
- **Updated**: `AGENTS.md` (added check_release_docs.py command)

## Acceptance criteria

- [x] Performance baseline links are consistent and resolve.
- [x] Final parity report has no misleading subgroup counts.
- [x] Hosted-CI caveat is prominent in release-facing docs.
- [x] Generated JSON report policy is explicit.
- [x] Release-doc consistency check exists and passes.
- [x] Cleanup completion record exists (this file).
- [x] No new feature scope was introduced.

## Remaining pre-tag items

None. This cleanup was the last polish layer before tagging. The release
candidate is ready to be tagged per the version in `PARITY_TARGET_FREEZE.md`.
