# Upstream Test Fixtures

This directory will contain immutable copies of upstream pproxy test files
for differential testing. Test fixtures are hash-checked and executed against
both oracle and candidate environments.

Currently no upstream test fixtures are included pending license review.
